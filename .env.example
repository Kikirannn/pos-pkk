# ==============================================
# APPLICATION CONFIGURATION
# ==============================================

# Application name (akan muncul di title, dll)
APP_NAME="Laravel POS - Kantin SMK"

# Environment: local, production, testing
APP_ENV=local

# Debug mode: true (development), false (production)
# WARNING: NEVER set true in production!
APP_DEBUG=true

# Application URL (sesuaikan dengan domain Anda)
APP_URL=http://localhost

# Application key (generate dengan: php artisan key:generate)
APP_KEY=

# ==============================================
# DATABASE CONFIGURATION
# ==============================================

# Database connection type
DB_CONNECTION=mysql

# Database host
DB_HOST=127.0.0.1

# Database port (default MySQL: 3306)
DB_PORT=3306

# Database name
DB_DATABASE=laravel_pos

# Database username
DB_USERNAME=root

# Database password
DB_PASSWORD=

# ==============================================
# CACHE & SESSION CONFIGURATION
# ==============================================

# Cache driver: file, redis, memcached, array
CACHE_DRIVER=file

# Session driver: file, cookie, database, redis
SESSION_DRIVER=file

# Session lifetime (in minutes)
SESSION_LIFETIME=120

# Queue connection: sync, database, redis
QUEUE_CONNECTION=sync

# ==============================================
# LOGGING CONFIGURATION
# ==============================================

# Log channel: stack, single, daily, slack, stderr
LOG_CHANNEL=stack

# Log level: debug, info, notice, warning, error, critical, alert, emergency
LOG_LEVEL=debug

# ==============================================
# MAIL CONFIGURATION (Optional - untuk notifications)
# ==============================================

MAIL_MAILER=smtp
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@kantinsmk.com"
MAIL_FROM_NAME="${APP_NAME}"

# ==============================================
# BROADCAST CONFIGURATION (Optional - untuk real-time)
# ==============================================

BROADCAST_DRIVER=log
# For production with real-time features, consider using 'pusher' or 'redis'

# ==============================================
# FILESYSTEM CONFIGURATION
# ==============================================

# Default filesystem disk: local, public, s3
FILESYSTEM_DISK=local

# ==============================================
# CUSTOM POS CONFIGURATION
# ==============================================

# Polling interval untuk kitchen display (milliseconds)
# Default: 3000ms (3 detik)
# Semakin kecil = semakin real-time tapi beban server naik
POS_POLLING_INTERVAL=3000

# Idle timeout untuk kasir tablet (milliseconds)
# Default: 30000ms (30 detik)
# Waktu tunggu sebelum reset otomatis ke halaman awal jika tidak ada aktivitas
POS_IDLE_TIMEOUT=30000

# Auto-remove done orders delay (milliseconds)
# Default: 120000ms (2 menit)
# Lama waktu pesanan berstatus 'Done' tetap tampil di layar dapur sebelum hilang
POS_AUTO_REMOVE_DELAY=120000

# Order number prefix (format: PREFIX + sequential number)
# Contoh: "ORDER-" -> "ORDER-001"
POS_ORDER_PREFIX=""

# Maximum order items per transaction
# Batas jumlah item dalam satu kali transaksi untuk mencegah spam/error
POS_MAX_ITEMS_PER_ORDER=50

# Enable sound notification (true/false)
# Mengaktifkan suara 'ting' saat pesanan baru masuk di dapur
POS_SOUND_NOTIFICATION=true

# ==============================================
# THIRD-PARTY SERVICES (Optional)
# ==============================================

# AWS S3 (jika gunakan cloud storage)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=
# AWS_USE_PATH_STYLE_ENDPOINT=false

# Pusher (jika gunakan real-time broadcasting)
# PUSHER_APP_ID=
# PUSHER_APP_KEY=
# PUSHER_APP_SECRET=
# PUSHER_APP_CLUSTER=mt1

# ==============================================
# PRODUCTION SETTINGS EXAMPLE (Commented)
# ==============================================

# Gunakan setting ini sebagai referensi saat deploy ke production:

# APP_ENV=production
# APP_DEBUG=false
# APP_URL=https://pos.yourdomain.com

# Database production (gunakan user dengan privilege minimal)
# DB_HOST=127.0.0.1
# DB_DATABASE=production_pos_db
# DB_USERNAME=pos_user
# DB_PASSWORD=strong_password_here

# Cache & Session production (gunakan Redis untuk performance terbaik)
# CACHE_DRIVER=redis
# SESSION_DRIVER=redis
# QUEUE_CONNECTION=redis
# REDIS_HOST=127.0.0.1
# REDIS_PASSWORD=null
# REDIS_PORT=6379

# ==============================================
# SECURITY NOTES
# ==============================================

# 1. NEVER commit .env file to version control (git)
# 2. Use strong passwords for database in production
# 3. Always generate unique APP_KEY: php artisan key:generate
# 4. Set APP_DEBUG=false di production untuk menyembunyikan stack trace error
# 5. Use HTTPS (SSL) di production untuk keamanan data
# 6. Restrict database user privileges (jangan gunakan user root di production)
# 7. Lakukan backup database secara berkala
# 8. Monitor logs di storage/logs/laravel.log secara berkala

# ==============================================
# QUICK START GUIDE
# ==============================================

# 1. Copy file ini menjadi .env:
#    cp .env.example .env
#
# 2. Edit konfigurasi database (DB_*) sesuai environment Anda
#
# 3. Generate application key:
#    php artisan key:generate
#
# 4. Migrate tabel dan seed data awal:
#    php artisan migrate --seed
#
# 5. Start development server:
#    php artisan serve
